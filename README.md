# Harts Hunches

Static GitHub Pages site.

## Structure

- `index.html` — page content
- `css/styles.css` — styling
- `images/hart.jpg` — portrait

Upload these files and folders to the root of the GitHub Pages repository.
